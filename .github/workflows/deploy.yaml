name: Deploy pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup AWS CLI
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-central-1

      - name: Build and push Docker image to ECR
        run: |
          # Log in to Amazon ECR
          aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/s3f1r6q9

          # Build and tag Docker image
          docker build -t version-backend-production .
          docker tag version-backend-production:latest public.ecr.aws/s3f1r6q9/version-backend-production:latest

          # Push Docker image to ECR
          docker push public.ecr.aws/s3f1r6q9/version-backend-production:latest
